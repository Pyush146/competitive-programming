#include<bits/stdc++.h>
using namespace std;
int main()
{
    long t;
    cin>>t;
    while(t--)
    {
        long long i,n,k;
        cin>>n>>k;
        string s;
        long long a[n];
        for(i=0;i<n;i++)
        {
            cin>>a[i];
        }
        long long r=0,sum=a[0];
        for(i=1;i<n;i++)
        {
            sum+=a[i];
            if(a[i]*100/sum>k)
            {
                r=rrr
            }
        }
    }
    cout<<endl;
}